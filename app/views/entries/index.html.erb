    <%= link_to("New entry", new_entry_path) %> | <%= link_to("Home", root_path)%>
 
  <% for entry in @entries %>
      <h2><%= link_to(entry.title, entry_path(entry))%></h2>
      <p><%= link_to("edit this entry", edit_entry_path(entry))%></p>
      <p><%= entry.date %> </p>
      <p><%= entry.contents%></p>
    <% end %>
